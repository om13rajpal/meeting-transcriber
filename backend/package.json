{
  "name": "meeting-transcriber-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone transcription backend (upload, ffmpeg extraction, Deepgram) for the Meeting Transcriber app, deployed separately from the Next.js frontend since it needs ffmpeg and large request bodies that Vercel's serverless functions cannot support.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.9.0",
    "multer": "^2.0.0",
    "resend": "^6.24.0"
  }
}
